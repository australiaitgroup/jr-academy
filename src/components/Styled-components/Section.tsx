import { widths } from '@app/styles/variables';
import styled from 'styled-components';

const Section = styled.section`
	display: relative;
	max-width: ${widths.max}px;
	margin: auto;
`;

export default Section;
